interface:
  display_name: "Install Argo MCP"
  short_description: "Connect Argo MCP in Codex or other OpenAI-compatible clients"
  default_prompt: "Use $install-argo-mcp to connect the hosted Argo MCP server to my client and verify campaign access."

dependencies:
  tools:
    - type: "mcp"
      value: "argo"
      description: "Argo MCP server"
      transport: "streamable_http"
      url: "https://mcp.argo.games/mcp"
